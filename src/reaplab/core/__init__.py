"""Core contracts: config models, record schemas, providers, hashing, state, paths.

Every other component (datagen, prune, evalharness, orchestrate, cli) builds
against the interfaces in this package. Changes here ripple everywhere —
keep them additive.
"""
